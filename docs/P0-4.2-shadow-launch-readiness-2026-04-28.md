# P0-4.2 Wave-3-A Shadow Mode 启动就绪记录

**生成日期**: 2026-04-28
**实施 agent**: wave-3-A (codex/bybit, R125-R131 commit 集合)
**前置阅读**: `docs/P0-4.2-implementation-roadmap-2026-04-27.md` §A 启动门禁清单

---

## §A.1 Bybit Demo `orderLinkId` 真测 — placeholder

**状态**: **SKIP**（user 未提供 Demo API key + secret）

**约束**: 路线图 §A.1 J.2 拍板要求"先按 idempotent return 假设设计；wave-3 启动前必须实测确认"。本步暂时按假设设计推进；F.3 (wave-3-C) Demo 模式启动前必须先把 §A.1 真测脚本跑完，并把响应原文（脱敏后）回填到 `docs/P0-4.2-bybit-orderlinkid-probe-2026-04-XX.md`。

**未实测的影响范围**:
- F.1 (本 wave) Shadow Mode 不下单，不依赖 link_id 复用语义 → **不阻塞**。
- F.2 (wave-3-B) Paper 模式不下交易所单 → **不阻塞**。
- F.3 (wave-3-C) Demo 模式真实 RPC → **必须前置完成**。

**通过线 (Wave-3-C 启动前必须勾掉)**:
- [ ] 实测响应 JSON 已记录
- [ ] 行为符合"idempotent return"或已在备用方案文档中标注
- [ ] WS `updateTime` 与 REST `dispatched_at` 的时差中位数 < 2 秒

---

## §A.2 7 天稳定性 checklist — partial SKIP

**状态**: **SKIP（wave-2 完成不到 24h）**

Wave-2 R118-R124 在 codex/bybit 上推进，距 wave-3-A 启动不足 24 小时；7 天稳定窗口暂未满足。本 wave-3-A 仍可推进，因为 Shadow Mode 自身不下任何真实单（旁支观察），其风险等级低于切实际执行路径。Wave-3-B/C/D 启动前应等观察期满。

**已确认**:
- [x] codex/bybit baseline `git log -1` = `671bc8e Round 123` (匹配 roadmap 期望，注：roadmap 写的 31afd24 R122 在 d34fb31 R124 之前)
- [x] 现有 876 pytest tests + 696 unittest tests 全绿（baseline 复跑确认）
- [x] persistence benchmark 已在 R121 完成（`docs/P0-5.1-perf-benchmark-2026-04-27.md` 1M 行 P95 < 100ms）

**Wave-3-B 启动前补完**:
- [ ] codex/bybit 在 main 之上 7 天稳定推进 (距 R124 7 天后)
- [ ] audit 24h ERROR 数 < 5
- [ ] persistence benchmark P95 复跑 < 100ms

---

## §A.3 verify-run baseline 确认

**状态**: ✅ **OK**

- [x] 当前 HEAD `671bc8e` (R123) 是稳定 baseline
- [x] `python3 -m pytest tests/ -q` 876/876 通过
- [x] `python3 -m pytest tests/test_persistence.py -q` 全绿（C2 SQLite + ConfigDao 落地稳定）
- [x] `python3 -m pytest tests/test_control_api.py -k "execution_engine"` 0 collected（确认 main.py 执行链路尚未被动过）
- [x] audit_events 中无 `execution.shadow_decision` 历史行（确认 shadow 数据空白）

---

## Wave-3-A 落地结论

按上述 baseline 推进 R125-R131 commit 集合。Shadow Mode 旁支调用 + audit 写入完成后，本文件将由 `docs/P0-4.2-shadow-observation-2026-04-XX.md` 接力（观察期记录）。
