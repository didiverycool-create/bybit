# Claude 接手清单

更新时间：2026-04-19（Asia/Shanghai）

本文档是给新 Claude 线程的补充接手材料。
权威来源仍然是当前仓库代码 + `AGENTS.md` + `README.md` + `docs/架构总览.md` + `docs/接口契约.md` + `docs/开发约定.md` + `docs/实施路线与接手清单.md`。
如果本文档与代码实现冲突，以代码和上述文档为准。

---

## 1. 先遵守的硬约束

- 用户沟通默认中文。
- 涉及网页入口、登录、账户设置、API Key 创建、网页端手动交易等，一律按 `https://www.bybit-global.com/` 表述，不要默认引导到 `bybit.com`。
- 交付节奏必须是“先实现，再测试，测试通过后再交付”。
- 不要把旧线程口头计划当事实，以当前仓库代码和文档为准。
- 当前工作区很脏，而且没有 commit / push / PR，不要随便清理、回滚或重置别人改动。
- 两条 Electron 校验不要并行跑；`verify:market-switch` 和 `verify:strategy-activity` 并行会抢 `5173` 端口，之前已经踩过一次。

---

## 2. 当前工作区状态

截至 2026-04-19，工作区仍是大面积脏状态，前后端都有很多本地改动和新增文件。
这不是“少量可忽略噪音”，而是一个明确的“多人/多轮拆分并行进行中”的工作区。

接手时要特别注意：

- 不要假设只有你要动的那几个文件被修改。
- 不要为了“看起来干净”去回滚大量 `M` / `??` 文件。
- 如果你选择推进某一条主线，优先只聚焦你负责的那组文件。
- 当前 `api.ts` 周边已经出现一批未提交的新拆分文件，例如：
  - `apps/desktop/src/apiHttp.ts`
  - `apps/desktop/src/apiMarket.ts`
  - `apps/desktop/src/apiStrategy.ts`
  - `apps/desktop/src/apiControlRuntime.ts`
  - `apps/desktop/src/apiOpsMonitoring.ts`
  - `apps/desktop/src/apiAiWorkflow.ts`
  - `apps/desktop/src/apiAccountTrading.ts`
  - `apps/desktop/src/apiSystemIntegration.ts`
  - `apps/desktop/src/apiFallbacks.ts`
- 这说明 `api.ts` 拆分很可能已经在部分进行中；如果你接下来要处理 API 层，先审一遍现状，不要平地再起一版。

---

## 3. 当前主线的实际进度

### 3.1 顶层壳已经明显压薄

当前几个关键文件的体量：

- `apps/desktop/src/App.tsx`：33 行
- `apps/desktop/src/components/useRuntimeAndSettingsModel.ts`：31 行
- `apps/desktop/src/components/buildRuntimeAndSettingsDerivedState.ts`：51 行
- `apps/desktop/src/components/buildRuntimeAndSettingsRuntimeState.ts`：50 行
- `apps/desktop/src/components/buildRuntimeAndSettingsSelectionState.ts`：49 行
- `apps/desktop/src/components/buildWorkspaceStrategySectionActions.ts`：22 行
- `apps/desktop/src/components/buildWorkspaceOpsSectionActions.ts`：39 行

这说明主线已经不是“继续拆 `App.tsx` 本体”，而是继续治理它下面的组合层、builder 爆炸和壳层微文件结构。

### 3.2 已经明确完成、不要重复做的展示层拆分

Claude 之前已完成的并行任务 A-D，不要重做：

- 策略执行上下文区拆分
  - `apps/desktop/src/components/StrategyExecutionContextSection.tsx`
  - `apps/desktop/src/components/strategy-execution/`
- 回测工作台区拆分
  - `apps/desktop/src/components/BacktestWorkspaceSection.tsx`
  - `apps/desktop/src/components/backtest-workspace/`
- 设置页拆分
  - `apps/desktop/src/components/SettingsSection.tsx`
  - `apps/desktop/src/components/settings-section/`
- 策略活动顶部动作区 + 决策分区拆分
  - `apps/desktop/src/components/StrategyActivityTopDecisionActionsSection.tsx`
  - `apps/desktop/src/components/StrategyActivityDecisionSections.tsx`
  - `apps/desktop/src/components/strategy-activity-sections/`

### 3.3 用户这边已完成的主线拆分

这些是当前主线已落地的前置拆分，不要回退：

- `apps/desktop/src/components/useWorkspaceDraftModel.ts`
- `apps/desktop/src/components/useControlDataQueryModel.ts`
- `apps/desktop/src/components/useStrategyReviewCatalogModel.ts`
- `apps/desktop/src/components/useManualTradePreviewModel.ts`
- `apps/desktop/src/components/useControlRefreshActions.ts`
- `apps/desktop/src/components/useRuntimeAndSettingsModel.ts`

---

## 4. 这几轮新落地的内容

下面这些是最近几轮真正已经做完并通过验证的内容。

### 4.1 App shell / 壳层装配继续压薄

已落地：

- `apps/desktop/src/components/AppWorkspaceShell.tsx`
- `apps/desktop/src/components/useAppShellModel.ts`
- `apps/desktop/src/components/buildAppShellModelSourceGroups.ts`
- `apps/desktop/src/components/buildAppPresentationModelsArgs.ts`
- `apps/desktop/src/components/buildAppPresentationModelsSourceGroups.ts`

结果：

- `App.tsx` 已经变成纯装配入口。
- 继续拆分时，重点不该再放在 `App.tsx`，而应放到其下层的 shell/composition/presentation builders。

### 4.2 工作区动作总线 / 导航总线拆分

#### 已落地的 action bus 拆分

策略侧：

- `apps/desktop/src/components/buildWorkspaceStrategySectionActions.ts`
- `apps/desktop/src/components/buildStrategyWorkspaceSectionActions.ts`
- `apps/desktop/src/components/buildBacktestWorkspaceSectionActions.ts`
- `apps/desktop/src/components/buildReplayWorkspaceSectionActions.ts`

ops 侧：

- `apps/desktop/src/components/buildWorkspaceOpsSectionActions.ts`
- `apps/desktop/src/components/buildWorkspaceSchedulerSectionActions.ts`
- `apps/desktop/src/components/buildWorkspaceOverviewSectionActions.ts`
- `apps/desktop/src/components/buildWorkspaceSettingsSectionActions.ts`
- `apps/desktop/src/components/buildWorkspaceMarketSectionActions.ts`
- `apps/desktop/src/components/buildWorkspaceAlertsSectionActions.ts`
- `apps/desktop/src/components/buildWorkspaceTradesSectionActions.ts`
- `apps/desktop/src/components/buildWorkspaceAuditSectionActions.ts`
- `apps/desktop/src/components/buildWorkspaceNewsSectionActions.ts`

#### 已落地的 detail navigation 拆分

- `apps/desktop/src/components/buildWorkspaceDetailNavigationActions.ts`
- `apps/desktop/src/components/buildWorkspaceDetailNavigationTransitions.ts`
- `apps/desktop/src/components/workspaceDetailNavigationShared.ts`
- `apps/desktop/src/components/resolveWorkspaceDetailNavigationState.ts`

结果：

- “动作总线”已经不再是单文件内联大块。
- “详情导航”里的 resolver / transition / action 责任也已经拆开。

### 4.3 runtime/settings 派生状态簇拆分

这一块是最近最后推进的主线。

已落地：

- `apps/desktop/src/components/useRuntimeAndSettingsModel.ts`
- `apps/desktop/src/components/useRuntimeAndSettingsDraftSync.ts`
- `apps/desktop/src/components/settingsDraftSyncHelpers.ts`
- `apps/desktop/src/components/buildRuntimeAndSettingsDerivedState.ts`
- `apps/desktop/src/components/buildRuntimeAndSettingsRuntimeState.ts`
- `apps/desktop/src/components/buildRuntimeAndSettingsSelectionState.ts`

当前结构已经变成：

- `useRuntimeAndSettingsModel.ts`
  - 只做顶层编排
- `useRuntimeAndSettingsDraftSync.ts`
  - 只做 settings draft 同步 effect
- `settingsDraftSyncHelpers.ts`
  - 统一“从 settings 生成 draft + 更新 signature ref”
- `buildRuntimeAndSettingsRuntimeState.ts`
  - 只做 `runtimeWorkerStatus / snapshotExecutionHealth / runtimeWorkerNeedsRecovery / runtimeWorkerRestoreHint / aiActivityFeed`
- `buildRuntimeAndSettingsSelectionState.ts`
  - 只做 `watchlistAlertSignature / selectedWatchItem / selectedStrategyRuntimePreview`
- `buildRuntimeAndSettingsDerivedState.ts`
  - 只做 merge + `settingsDraftDirty / desktopNotificationsEnabled`

这条线已经明显收口，但还没有到“彻底不用再碰”的程度。

### 4.4 Strategy Activity / 审计项 #6、#16 已有明确推进

#### 审计 #6：`StrategyActivitySnapshot` 过胖，已做的收缩

已经落地的关键变化：

- 后端不再输出顶层重复的 `latest_ops` flat 字段。
- 前端也已经移除对顶层 `latest_*` flat 字段的兼容依赖，改为只认嵌套 `latest_ops` / `latest_runtime`。
- 前端 normalization 命名也做过一轮收口，把 `FlatFields` 重新命名为 `DerivedState` / `LatestOpsState` 方向。

关键文件包括：

- `services/control-api/models.py`
- `services/control-api/strategy_activity_snapshot_kwargs_helpers.py`
- `services/control-api/tests/test_control_api.py`
- `apps/desktop/src/types.ts`
- `apps/desktop/src/strategyActivityApiHelpers.ts`
- `apps/desktop/src/strategyActivityLatestOpsSnapshot.ts`
- `apps/desktop/src/strategyActivitySnapshotDefaults.ts`
- `apps/desktop/src/strategyActivitySnapshotNormalization.ts`
- `apps/desktop/src/strategyActivitySnapshotNormalizationDerivedState.ts`
- `apps/desktop/src/strategyActivitySnapshotNormalizationDerivedState.types.ts`
- `apps/desktop/src/strategyActivitySnapshotNormalizationDerivedStateResolvers.ts`
- `apps/desktop/src/strategyActivitySnapshotNormalizationLatestOpsState.ts`

#### 审计 #16：`build_strategy_activity_payload` 多轮遍历，已做的优化

已经落地的关键变化：

- `latest_retryable_tracking_job_record` 已在首次遍历 `job summaries` 时直接带出，不再为找 retryable record 再扫一次。
- `recent_reviews` 的双遍历已经各在 lineage map 和 review-context index 里收成单次遍历。

关键文件包括：

- `services/control-api/strategy_activity_job_summary_helpers.py`
- `services/control-api/strategy_activity_summary_types.py`
- `services/control-api/strategy_activity_summary_helpers.py`
- `services/control-api/strategy_activity_payload_lineage_helpers.py`
- `services/control-api/strategy_activity_lineage_maps_helpers.py`
- `services/control-api/strategy_activity_review_context_indexes_helpers.py`

这些优化都已经通过后端相关单测和桌面回归，不要回退。

---

## 5. 最新验证基线

下面是当前可以视为“接手基线”的最新验证状态。

### 5.1 桌面端必跑基线

以下命令最新都通过：

```bash
pnpm --dir apps/desktop lint
pnpm --dir apps/desktop build
pnpm -C /Users/leo/Desktop/Work/bybit verify:market-switch
pnpm -C /Users/leo/Desktop/Work/bybit verify:strategy-activity
```

最新结果：

- `verify:market-switch`
  - `status=ok`
  - `attention=stable`
  - `stable_runs=19`
- `verify:strategy-activity`
  - `status=ok`
  - `verify_regression=pass`
  - `verify_attention=stable`
  - `smoke_regression=pass`
  - `smoke_attention=stable`
  - `sections=6/6`
  - `top_recent=5`
  - `notes=5`

### 5.2 之前已经跑过的后端关键验证

与 Strategy Activity 改动直接相关的后端测试也已经多次通过，典型包括：

```bash
python3 -m unittest tests.test_control_api.ControlApiIntegrationTests.test_strategy_activity_endpoint_returns_recent_strategy_activity
python3 -m unittest tests.test_control_api.ControlApiIntegrationTests.test_review_strategy_activity_context_scans_primary_reviews_once
python3 -m unittest tests.test_control_api.ControlApiIntegrationTests.test_review_strategy_activity_context_uses_high_value_audit_summary
python3 -m unittest tests.test_control_api.ControlApiIntegrationTests.test_review_strategy_activity_context_includes_latest_alert_order_and_trade
python3 -m unittest tests.test_control_api.ControlApiIntegrationTests.test_strategy_activity_includes_retry_metadata_for_tracking_jobs
```

如果你继续动 `services/control-api/strategy_activity_*`，建议除了四项桌面基线外，再补跑这些定向测试。

### 5.3 当前仍存在但不是这轮新增的问题

- Vite 仍有 `chunk > 500kB` warning。
- 没有做人工 Electron 点点点视觉验收。

---

## 6. 当前主线还没做完的，接下来怎么接

### 6.1 最推荐的接法

#### 第一优先：继续前端主线，但不要再死磕 `App.tsx`

`App.tsx` 已经只有 33 行，本体已经不是主战场。

更有价值的继续点：

- 继续治理 shell/composition 周边的 builder / args / input 微文件膨胀
- 继续治理 `runtime/settings` 周边剩下的小段 merge/helper 结构
- 开始统一清理 `build*Args.ts` / `build*Input.ts` 这种双胞胎文件

我的判断是：

- 如果只是为了“再拆一刀”，`buildRuntimeAndSettingsDerivedState.ts` 里最多还值得再抽一个很薄的 `settings flags` helper。
- 但从收益比看，更值得开始转向：
  - `api.ts` 拆分收口
  - app shell / composition 的 builder 双胞胎治理
  - strategy activity normalization 微文件治理

#### 第二优先：处理审计里最明确、最小风险的项

按收益比，建议优先考虑：

1. `services/control-api/main.py` 里 `load_private_positions_snapshot()` 的死代码删除
2. Prometheus label 转义补测试
3. `api.ts` 拆分现状盘点和收口

### 6.2 如果继续按“当前前端主线”往下拆，推荐顺序

建议顺序：

1. 审 `buildRuntimeAndSettingsDerivedState.ts` 是否还值得再抽 `settings flags`
2. 若收益低，就转去 app shell / composition 层，优先找“文件很多但只是搬参数”的双胞胎 builder
3. 再往后，处理 `strategyActivitySnapshotNormalization*` 系列文件的合并/收口

不建议：

- 继续为了拆而拆，新增更多只被单处 import、且 20-30 行的小 helper
- 在当前工作区这么脏的情况下做大面积 rename/移动而不先固定目标范围

---

## 7. 审计报告状态：哪些已动，哪些没动

当前同时存在两份需要参考的审计材料：

- `code-review-report.md`
- `code-review-report-2026-04-18.md`

建议以 `code-review-report-2026-04-18.md` 作为新的增量审计入口，再结合当前代码核实。

### 7.1 已经明确推进过的

- #1 Prometheus label 注入：已修
- #2 App.tsx God Component：已明显修复，当前 33 行
- #4 `snapshot()` 读写副作用：已修
- #5 缓存线程安全：已修
- #6 `StrategyActivitySnapshot` 过胖：已部分收敛，仍未彻底完成
- #9 缓存容量上限：已修
- #10 K 线缓存 TTL：已修
- #16 `build_strategy_activity_payload` 多轮遍历：已部分缓解

### 7.2 仍然值得继续追的

- #3 Electron 注入 / `executeJavaScript(...)` 字符串脚本
- #6 Strategy Activity 结构仍偏胖
- #7 watchlist / market strict 错误态
- #8 SSE 断连兜底
- #11 前后端重复逻辑
- #12 ECharts lazy
- #14 `RLock` 重入链路
- #17 `pageUnloading`
- #18 smoke 链路硬编码地址
- #22 `build_runtime_market_fallback_detail` 的 `source="mock"` 误导
- #25 `allow_rest_refresh=False` 分支复核

### 7.3 `code-review-report-2026-04-18.md` 里的新问题

需要特别留意：

- `main.py` 存在一段 `return` 之后不可达、且引用未定义变量 `value` 的死代码
- 前端 `components/` 下 builder / args / input 微文件爆炸
- `api.ts` 职责混杂
- 测试覆盖仍明显不均衡

---

## 8. 接手时最容易踩坑的点

### 8.1 不要重做已完成拆分

尤其不要去重做：

- 展示层 A-D 拆分
- `App.tsx` 本体拆分
- 已完成的 workspace action bus / detail navigation 拆分
- 已完成的 runtime/settings 第一轮拆分
- 已完成的 Strategy Activity latest ops 去 flat 化

### 8.2 不要把 `latest_*` 顶层 flat 字段重新加回来

前端和后端都已经往“只认嵌套 `latest_ops` / `latest_runtime`”这个方向收敛了。
如果你后续改 Strategy Activity，不要再把顶层 `latest_order / latest_trade / latest_alert / latest_audit_event` 那种兼容字段重新扩回来。

### 8.3 不要并行跑两条 Electron verify

必须顺序：

```bash
pnpm -C /Users/leo/Desktop/Work/bybit verify:market-switch
pnpm -C /Users/leo/Desktop/Work/bybit verify:strategy-activity
```

### 8.4 不要默认 API 层还是单文件旧形态

当前工作区已经出现一批新的 `api*.ts` 文件，但还没有 commit。
这意味着：

- `api.ts` 可能正在被部分拆分
- 你接手时必须先看现状
- 不要再从零起一套平行拆分方案

### 8.5 不要在这时候搞大规模“清理工作区”

当前最危险的错误操作就是：

- `git checkout -- .`
- `git reset --hard`
- 大面积回滚陌生文件

这会直接把其他并行主线的本地成果一起抹掉。

---

## 9. 推荐的接手动作

建议 Claude 接手后的第一步顺序：

1. 先读：
   - `AGENTS.md`
   - `README.md`
   - `docs/架构总览.md`
   - `docs/接口契约.md`
   - `docs/开发约定.md`
   - `docs/实施路线与接手清单.md`
   - 本文档
2. 先审当前自己要碰的范围，不要先扫全仓所有脏文件
3. 选一条主线，不要同时跨前端壳层、API 拆分、后端策略活动三条线一起动
4. 每一轮按以下顺序交付：

```bash
pnpm --dir apps/desktop lint
pnpm --dir apps/desktop build
pnpm -C /Users/leo/Desktop/Work/bybit verify:market-switch
pnpm -C /Users/leo/Desktop/Work/bybit verify:strategy-activity
```

如果这轮动了 `services/control-api/strategy_activity_*`，再补相应 Python 单测。

---

## 10. 我对下一轮最具体的建议

如果 Claude 想低风险无缝接上，我建议从下面两个方案中选一个，不要混着来。

### 方案 A：继续前端主线，保持连续性

目标：

- 收掉 `runtime/settings` 最后一层薄 merge
- 然后转去壳层 builder / args / input 双胞胎治理

优点：

- 连续性最好
- 改动风险相对可控
- 桌面回归已经有很稳定的基线

### 方案 B：切到审计里最明确的小修

目标：

- 先删 `main.py` 死代码
- 再补 Prometheus label 单测

优点：

- 风险小
- 收益明确
- 不会继续恶化前端微文件爆炸

如果要我给一个偏实战的优先级判断：

1. 若继续主线开发，优先做方案 A
2. 若想先吃掉一个确定性审计项，再做方案 B

---

## 11. 一句话总结给 Claude

当前主线不是“继续拆 `App.tsx`”，而是“在不回退既有拆分成果的前提下，继续治理壳层 builder / args / input 微文件爆炸，并逐步把 Strategy Activity 和 API 层的结构收口”；接手时先锁定目标范围、顺序跑四项验证、绝对不要清理脏工作区。
