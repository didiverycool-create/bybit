function decodeSmokeScript(script: string): string {
  return script.replace(/\\([`$])/g, "$1").replace(/\s*`;\s*$/, "").trim();
}

const strategyActivitySetupScript = decodeSmokeScript("(async () => {\n              const apiBase = __API_BASE__;\n              const workspaceStorageKey = 'bybit-control-workspace-v1';\n              const phaseKey = 'bybit-smoke-strategy-activity-phase';\n              const fetchJson = async (path) => {\n                const response = await fetch(\\`\\${apiBase}\\${path}\\`);\n                const text = await response.text();\n                let payload = null;\n                try {\n                  payload = text ? JSON.parse(text) : null;\n                } catch (_error) {\n                  payload = text;\n                }\n                if (!response.ok) {\n                  throw new Error(\n                    typeof payload === 'object' && payload && 'detail' in payload\n                      ? String(payload.detail)\n                      : \\`HTTP \\${response.status}\\`,\n                  );\n                }\n                return payload;\n              };\n              const workspace = await fetchJson('/api/workspace/preferences');\n              const strategies = await fetchJson('/api/strategies');\n              const firstStrategy = Array.isArray(strategies) && strategies.length ? strategies[0] : null;\n              const nextWorkspace = {\n                ...workspace,\n                active_section: 'strategy',\n                selected_strategy_id: firstStrategy?.id ?? workspace?.selected_strategy_id ?? null,\n                selected_symbol:\n                  firstStrategy?.symbols?.[0] ??\n                  workspace?.selected_symbol ??\n                  'BTCUSDT',\n                selected_backtest_id: null,\n                selected_scheduler_job_id: null,\n                selected_strategy_detail_panel: 'activity',\n                selected_strategy_tracking_kind: 'issue',\n                selected_strategy_tracking_summary: '',\n                selected_strategy_tracking_detail: '',\n                selected_review_inspector_id: null,\n                selected_review_inspector_strategy_id: null,\n                selected_review_id: null,\n                selected_proposal_id: null,\n                selected_change_request_id: null,\n              };\n              localStorage.setItem(workspaceStorageKey, JSON.stringify(nextWorkspace));\n              sessionStorage.setItem(phaseKey, 'prepared');\n              return {\n                workspaceStorageKey,\n                phaseKey,\n                strategyId: nextWorkspace.selected_strategy_id,\n                symbol: nextWorkspace.selected_symbol,\n                strategyCount: Array.isArray(strategies) ? strategies.length : 0,\n              };\n            })()");
const strategyActivityReadStateScript = decodeSmokeScript("() => {\n              const panel = document.querySelector('[data-strategy-activity-panel=\"1\"]');\n              const panelBody = panel?.querySelector('[data-strategy-activity-body=\"1\"]');\n              const requiredSections = ['proposals', 'change-requests', 'backtests', 'tracking-reviews', 'primary-reviews', 'jobs'];\n              const actionGroups = [\n                'proposals',\n                'change-requests',\n                'backtests',\n                'tracking-reviews',\n                'primary-reviews',\n                'jobs',\n                'alerts',\n                'audit',\n                'active-orders',\n                'historical-orders',\n                'trades',\n              ];\n              const topActionGroups = [\n                'recent',\n                'audit',\n                'latest-focus',\n                'lineage',\n                'proposal-change',\n                'actionable',\n              ];\n              const distinctValues = (values) => {\n                const normalized = Array.isArray(values) ? values : [];\n                return normalized.reduce((acc, value) => {\n                  if (typeof value !== 'string') {\n                    return acc;\n                  }\n                  const trimmed = value.trim();\n                  if (!trimmed || acc.seen.has(trimmed)) {\n                    return acc;\n                  }\n                  acc.seen.add(trimmed);\n                  acc.items.push(trimmed);\n                  return acc;\n                }, { items: [], seen: new Set() }).items;\n              };\n              const duplicateValues = (values) => {\n                const counts = new Map();\n                for (const value of Array.isArray(values) ? values : []) {\n                  if (typeof value !== 'string') {\n                    continue;\n                  }\n                  const trimmed = value.trim();\n                  if (!trimmed) {\n                    continue;\n                  }\n                  counts.set(trimmed, (counts.get(trimmed) ?? 0) + 1);\n                }\n                return Array.from(counts.entries())\n                  .filter(([, count]) => count > 1)\n                  .map(([value]) => value);\n              };\n              const buildRowSemanticBinding = (section, node) => {\n                const rowId = node.getAttribute('data-strategy-activity-row-id') ?? '';\n                if (!rowId) {\n                  return '';\n                }\n                const rowTitle =\n                  node\n                    .querySelector('.console-row__main strong')\n                    ?.textContent?.trim() ?? '';\n                const rowSummary = Array.from(node.querySelectorAll('.console-row__main p'))\n                  .map((summaryNode) => summaryNode.textContent?.trim() ?? '')\n                  .filter(Boolean)\n                  .join(' ｜ ');\n                const rowActionLabels = distinctValues(\n                  Array.from(node.querySelectorAll('button'))\n                    .map((actionNode) => actionNode.textContent?.trim() ?? '')\n                    .filter(Boolean),\n                );\n                const rowActionKeys = distinctValues(\n                  rowActionLabels\n                    .map((label) => mapSectionActionKey(section, label))\n                    .filter(Boolean),\n                );\n                const rowUnknownActionLabels = distinctValues(\n                  rowActionLabels.filter((label) => !mapSectionActionKey(section, label)),\n                );\n                const rowBadgeLabels = distinctValues(\n                  Array.from(node.querySelectorAll('.console-tag--warn'))\n                    .map((badgeNode) => badgeNode.textContent?.trim() ?? '')\n                    .filter(Boolean),\n                );\n                const rowBadgeKeys = distinctValues(\n                  rowBadgeLabels\n                    .map((label) => mapSectionBadgeKey(section, label))\n                    .filter(Boolean),\n                );\n                const rowUnknownBadgeLabels = distinctValues(\n                  rowBadgeLabels.filter((label) => !mapSectionBadgeKey(section, label)),\n                );\n                return (\n                  rowId +\n                  ' => ' +\n                  JSON.stringify({\n                    title: rowTitle || null,\n                    summary: rowSummary || null,\n                    actions: rowActionKeys,\n                    unknownActions: rowUnknownActionLabels,\n                    badges: rowBadgeKeys,\n                    unknownBadges: rowUnknownBadgeLabels,\n                  })\n                );\n              };\n              const mapSectionActionKey = (section, label) => {\n                const normalizedLabel = (label || '').trim();\n                if (!normalizedLabel) {\n                  return null;\n                }\n                if (section === 'proposals') {\n                  if (normalizedLabel.startsWith('接受')) return 'accept';\n                  if (normalizedLabel === '拒绝') return 'reject';\n                  if (normalizedLabel === '打开提案') return 'open_proposal';\n                  if (normalizedLabel === '生成变更') return 'linked_change_request';\n                  if (normalizedLabel === '生成回测') return 'linked_backtest';\n                  if (normalizedLabel === '生成复盘') return 'linked_review';\n                  if (normalizedLabel === '打开任务') return 'open_job';\n                  if (normalizedLabel === '查看结果') return 'open_result';\n                  if (normalizedLabel === '重试跟踪') return 'retry_tracking';\n                  return null;\n                }\n                if (section === 'change-requests') {\n                  if (normalizedLabel === '查看变更') return 'open_change_request';\n                  if (normalizedLabel === '打开回测') return 'open_backtest';\n                  if (normalizedLabel === '按建议重跑') return 'rerun_recommended_backtest';\n                  if (normalizedLabel === '打开任务') return 'open_job';\n                  if (normalizedLabel === '查看结果') return 'open_result';\n                  if (normalizedLabel === '重试跟踪') return 'retry_tracking';\n                  if (normalizedLabel === '来源回测') return 'source_backtest';\n                  if (normalizedLabel === '来源复盘') return 'source_review';\n                  if (normalizedLabel === '来源提案') return 'source_proposal';\n                  return null;\n                }\n                if (section === 'backtests') {\n                  if (normalizedLabel === '打开回测') return 'open_backtest';\n                  if (normalizedLabel === '查看结果') return 'open_result';\n                  if (normalizedLabel === '打开任务') return 'open_job';\n                  if (normalizedLabel === '重试跟踪') return 'retry_tracking';\n                  if (normalizedLabel === '来源变更') return 'source_change_request';\n                  if (normalizedLabel === '来源回测') return 'source_backtest';\n                  if (normalizedLabel === '来源复盘') return 'source_review';\n                  if (normalizedLabel === '来源提案') return 'source_proposal';\n                  return null;\n                }\n                if (section === 'tracking-reviews') {\n                  if (normalizedLabel === '来源变更') return 'source_change_request';\n                  if (normalizedLabel === '来源回测') return 'source_backtest';\n                  if (normalizedLabel === '来源复盘') return 'source_review';\n                  if (normalizedLabel === '来源提案') return 'source_proposal';\n                  if (normalizedLabel === '打开任务') return 'open_job';\n                  if (normalizedLabel === '查看结果') return 'open_result';\n                  return null;\n                }\n                if (section === 'primary-reviews') {\n                  if (normalizedLabel === '来源变更') return 'source_change_request';\n                  if (normalizedLabel === '来源回测') return 'source_backtest';\n                  if (normalizedLabel === '来源复盘') return 'source_review';\n                  if (normalizedLabel === '来源提案') return 'source_proposal';\n                  if (normalizedLabel === '打开任务') return 'open_job';\n                  if (normalizedLabel === '打开复盘') return 'open_review';\n                  if (normalizedLabel === '按建议重跑') return 'rerun_recommended_backtest';\n                  return null;\n                }\n                if (section === 'jobs') {\n                  if (normalizedLabel === '查看结果') return 'open_result';\n                  if (normalizedLabel === '查看变更') return 'open_change_request';\n                  if (normalizedLabel === '打开回测') return 'open_backtest';\n                  if (normalizedLabel === '来源回测') return 'source_backtest';\n                  if (normalizedLabel === '来源复盘') return 'source_review';\n                  if (normalizedLabel === '来源提案') return 'source_proposal';\n                  if (normalizedLabel === '打开复盘') return 'open_review';\n                  if (normalizedLabel === '重试') return 'retry_job';\n                  return null;\n                }\n                if (section === 'alerts') {\n                  if (normalizedLabel === '提醒页') return 'open_alerts';\n                  if (normalizedLabel === '提醒行情') return 'open_market';\n                  if (normalizedLabel === '自选规则') return 'open_watchlist_rules';\n                  if (normalizedLabel === '确认提醒') return 'acknowledge';\n                  if (normalizedLabel === '恢复提醒') return 'restore';\n                  return null;\n                }\n                if (section === 'audit') {\n                  if (normalizedLabel === '审计页') return 'open_audit';\n                  if (normalizedLabel === '打开任务') return 'open_job';\n                  if (normalizedLabel === '查看结果') return 'open_result';\n                  if (normalizedLabel === '查看变更') return 'open_change_request';\n                  if (normalizedLabel === '打开回测') return 'open_backtest';\n                  if (normalizedLabel === '来源回测') return 'source_backtest';\n                  if (normalizedLabel === '来源复盘') return 'source_review';\n                  if (normalizedLabel === '来源提案') return 'source_proposal';\n                  return null;\n                }\n                if (section === 'active-orders') {\n                  if (normalizedLabel === '委托页') return 'open_orders';\n                  if (normalizedLabel === '委托行情') return 'open_market';\n                  if (normalizedLabel === '改单') return 'edit_order';\n                  if (normalizedLabel === '撤单') return 'cancel_order';\n                  return null;\n                }\n                if (section === 'historical-orders') {\n                  if (normalizedLabel === '委托页') return 'open_orders';\n                  if (normalizedLabel === '委托行情') return 'open_market';\n                  return null;\n                }\n                if (section === 'trades') {\n                  if (normalizedLabel === '成交页') return 'open_trades';\n                  if (normalizedLabel === '成交行情') return 'open_market';\n                  return null;\n                }\n                return null;\n              };\n              const mapSectionBadgeKey = (section, label) => {\n                const normalizedLabel = (label || '').trim();\n                if (!normalizedLabel) {\n                  return null;\n                }\n                if (normalizedLabel === '当前最新') return 'current_latest';\n                if (normalizedLabel === '当前可处理') return 'current_actionable';\n                if (normalizedLabel === '当前可重试') return 'current_retryable';\n                if (normalizedLabel === '当前聚焦') return 'current_focus';\n                if (normalizedLabel === '当前定位') return 'current_selected';\n                if (normalizedLabel === '需人工跟进') return 'manual_followup';\n                if (normalizedLabel === '当前提案') return 'current_proposal';\n                if (normalizedLabel === '当前变更') return 'current_change_request';\n                if (normalizedLabel === '当前回测') return 'current_backtest';\n                if (normalizedLabel === '当前复盘') return 'current_review';\n                if (normalizedLabel === '当前任务') return 'current_job';\n                if (normalizedLabel === '当前回测结果') return 'current_backtest_review';\n                if (normalizedLabel === '当前回测任务') return 'current_backtest_job';\n                if (normalizedLabel === '当前活跃') return 'current_active';\n                if (normalizedLabel === '当前最新历史') return 'current_latest_historical';\n                if (normalizedLabel === '当前待处理') return 'current_pending';\n                return null;\n              };\n              const sections = Object.fromEntries(\n                requiredSections.map((section) => [\n                  section,\n                  Boolean(panel?.querySelector(\\`[data-strategy-activity-section=\"\\${section}\"]\\`)),\n                ]),\n              );\n              const labels = Object.fromEntries(\n                requiredSections.map((section) => [\n                  section,\n                  panel?.querySelector(\\`[data-strategy-activity-section-label=\"\\${section}\"]\\`)?.textContent?.trim() ?? null,\n                ]),\n              );\n              const sectionActionLabelsBySection = Object.fromEntries(\n                actionGroups.map((section) => {\n                  const buttons = Array.from(\n                    panel?.querySelectorAll(\\`[data-strategy-activity-action-group=\"\\${section}\"] button\\`) ?? [],\n                  );\n                  const labels = distinctValues(\n                    buttons\n                      .map((node) => node.textContent?.trim() ?? '')\n                      .filter(Boolean),\n                  );\n                  return [section, labels];\n                }),\n              );\n              const sectionActionKeysBySection = Object.fromEntries(\n                actionGroups.map((section) => [\n                  section,\n                  distinctValues(\n                    (sectionActionLabelsBySection[section] ?? [])\n                      .map((label) => mapSectionActionKey(section, label))\n                      .filter(Boolean),\n                  ),\n                ]),\n              );\n              const sectionUnknownActionLabelsBySection = Object.fromEntries(\n                actionGroups.map((section) => [\n                  section,\n                  distinctValues(\n                    (sectionActionLabelsBySection[section] ?? []).filter(\n                      (label) => !mapSectionActionKey(section, label),\n                    ),\n                  ),\n                ]),\n              );\n              const sectionBadgeLabelsBySection = Object.fromEntries(\n                actionGroups.map((section) => {\n                  const badges = Array.from(\n                    panel?.querySelectorAll(\\`[data-strategy-activity-action-group=\"\\${section}\"] .console-tag--warn\\`) ?? [],\n                  );\n                  const labels = distinctValues(\n                    badges\n                      .map((node) => node.textContent?.trim() ?? '')\n                      .filter(Boolean),\n                  );\n                  return [section, labels];\n                }),\n              );\n              const sectionBadgeKeysBySection = Object.fromEntries(\n                actionGroups.map((section) => [\n                  section,\n                  distinctValues(\n                    (sectionBadgeLabelsBySection[section] ?? [])\n                      .map((label) => mapSectionBadgeKey(section, label))\n                      .filter(Boolean),\n                  ),\n                ]),\n              );\n              const sectionUnknownBadgeLabelsBySection = Object.fromEntries(\n                actionGroups.map((section) => [\n                  section,\n                  distinctValues(\n                    (sectionBadgeLabelsBySection[section] ?? []).filter(\n                      (label) => !mapSectionBadgeKey(section, label),\n                    ),\n                  ),\n                ]),\n              );\n              const sectionRowCounts = Object.fromEntries(\n                actionGroups.map((section) => [\n                  section,\n                  panel?.querySelectorAll(\\`[data-strategy-activity-action-group=\"\\${section}\"] .trade-row\\`)?.length ?? 0,\n                ]),\n              );\n              const sectionRawRowPrimaryTextsBySection = Object.fromEntries(\n                actionGroups.map((section) => [\n                  section,\n                  Array.from(\n                    panel?.querySelectorAll(\n                      \\`[data-strategy-activity-action-group=\"\\${section}\"] .trade-row\\`,\n                    ) ?? [],\n                  )\n                    .map((node) =>\n                      node\n                        .querySelector('.console-row__main strong')\n                        ?.textContent?.trim() ?? '',\n                    )\n                    .filter(Boolean),\n                ]),\n              );\n              const sectionRawRowIdsBySection = Object.fromEntries(\n                actionGroups.map((section) => [\n                  section,\n                  Array.from(\n                    panel?.querySelectorAll(\n                      \\`[data-strategy-activity-action-group=\"\\${section}\"] .trade-row[data-strategy-activity-row-id]\\`,\n                    ) ?? [],\n                  )\n                    .map((node) => node.getAttribute('data-strategy-activity-row-id') ?? '')\n                    .filter(Boolean),\n                ]),\n              );\n              const sectionStructuredRowIdCountsBySection = Object.fromEntries(\n                actionGroups.map((section) => [\n                  section,\n                  sectionRawRowIdsBySection[section]?.length ?? 0,\n                ]),\n              );\n              const sectionStructuredRowTitleCountsBySection = Object.fromEntries(\n                actionGroups.map((section) => [\n                  section,\n                  sectionRawRowPrimaryTextsBySection[section]?.length ?? 0,\n                ]),\n              );\n              const sectionRawRowTitleBindingsBySection = Object.fromEntries(\n                actionGroups.map((section) => [\n                  section,\n                  Array.from(\n                    panel?.querySelectorAll(\n                      \\`[data-strategy-activity-action-group=\"\\${section}\"] .trade-row[data-strategy-activity-row-id]\\`,\n                    ) ?? [],\n                  )\n                    .map((node) => {\n                      const rowId = node.getAttribute('data-strategy-activity-row-id') ?? '';\n                      const rowTitle =\n                        node\n                          .querySelector('.console-row__main strong')\n                          ?.textContent?.trim() ?? '';\n                      return rowId && rowTitle ? \\`\\${rowId} => \\${rowTitle}\\` : '';\n                    })\n                    .filter(Boolean),\n                ]),\n              );\n              const sectionStructuredRowTitleBindingCountsBySection = Object.fromEntries(\n                actionGroups.map((section) => [\n                  section,\n                  sectionRawRowTitleBindingsBySection[section]?.length ?? 0,\n                ]),\n              );\n              const sectionRawRowSummaryBindingsBySection = Object.fromEntries(\n                actionGroups.map((section) => [\n                  section,\n                  Array.from(\n                    panel?.querySelectorAll(\n                      \\`[data-strategy-activity-action-group=\"\\${section}\"] .trade-row[data-strategy-activity-row-id]\\`,\n                    ) ?? [],\n                  )\n                    .map((node) => {\n                      const rowId = node.getAttribute('data-strategy-activity-row-id') ?? '';\n                      const rowSummary = Array.from(node.querySelectorAll('.console-row__main p'))\n                        .map((summaryNode) => summaryNode.textContent?.trim() ?? '')\n                        .filter(Boolean)\n                        .join(' ｜ ');\n                      return rowId && rowSummary ? \\`\\${rowId} => \\${rowSummary}\\` : '';\n                    })\n                    .filter(Boolean),\n                ]),\n              );\n              const sectionStructuredRowSummaryBindingCountsBySection = Object.fromEntries(\n                actionGroups.map((section) => [\n                  section,\n                  sectionRawRowSummaryBindingsBySection[section]?.length ?? 0,\n                ]),\n              );\n              const sectionRawRowActionBindingsBySection = Object.fromEntries(\n                actionGroups.map((section) => [\n                  section,\n                  Array.from(\n                    panel?.querySelectorAll(\n                      \\`[data-strategy-activity-action-group=\"\\${section}\"] .trade-row[data-strategy-activity-row-id]\\`,\n                    ) ?? [],\n                  )\n                    .map((node) => {\n                      const rowId = node.getAttribute('data-strategy-activity-row-id') ?? '';\n                      const rowActionKeys = distinctValues(\n                        Array.from(node.querySelectorAll('button'))\n                          .map((actionNode) => mapSectionActionKey(section, actionNode.textContent?.trim() ?? ''))\n                          .filter(Boolean),\n                      );\n                      return rowId && rowActionKeys.length ? \\`\\${rowId} => \\${rowActionKeys.join(', ')}\\` : '';\n                    })\n                    .filter(Boolean),\n                ]),\n              );\n              const sectionStructuredRowActionBindingCountsBySection = Object.fromEntries(\n                actionGroups.map((section) => [\n                  section,\n                  sectionRawRowActionBindingsBySection[section]?.length ?? 0,\n                ]),\n              );\n              const sectionRawRowBadgeLabelBindingsBySection = Object.fromEntries(\n                actionGroups.map((section) => [\n                  section,\n                  Array.from(\n                    panel?.querySelectorAll(\n                      \\`[data-strategy-activity-action-group=\"\\${section}\"] .trade-row[data-strategy-activity-row-id]\\`,\n                    ) ?? [],\n                  )\n                    .map((node) => {\n                      const rowId = node.getAttribute('data-strategy-activity-row-id') ?? '';\n                      const rowBadgeLabels = distinctValues(\n                        Array.from(node.querySelectorAll('.console-tag--warn'))\n                          .map((badgeNode) => badgeNode.textContent?.trim() ?? '')\n                          .filter(Boolean),\n                      );\n                      return rowId && rowBadgeLabels.length ? \\`\\${rowId} => \\${rowBadgeLabels.join(', ')}\\` : '';\n                    })\n                    .filter(Boolean),\n                ]),\n              );\n              const sectionRawRowUnknownBadgeBindingsBySection = Object.fromEntries(\n                actionGroups.map((section) => [\n                  section,\n                  Array.from(\n                    panel?.querySelectorAll(\n                      \\`[data-strategy-activity-action-group=\"\\${section}\"] .trade-row[data-strategy-activity-row-id]\\`,\n                    ) ?? [],\n                  )\n                    .map((node) => {\n                      const rowId = node.getAttribute('data-strategy-activity-row-id') ?? '';\n                      const rowUnknownBadgeLabels = distinctValues(\n                        Array.from(node.querySelectorAll('.console-tag--warn'))\n                          .map((badgeNode) => badgeNode.textContent?.trim() ?? '')\n                          .filter(Boolean)\n                          .filter((label) => !mapSectionBadgeKey(section, label)),\n                      );\n                      return rowId && rowUnknownBadgeLabels.length\n                        ? \\`\\${rowId} => \\${rowUnknownBadgeLabels.join(', ')}\\`\n                        : '';\n                    })\n                    .filter(Boolean),\n                ]),\n              );\n              const sectionRawRowBadgeBindingsBySection = Object.fromEntries(\n                actionGroups.map((section) => [\n                  section,\n                  Array.from(\n                    panel?.querySelectorAll(\n                      \\`[data-strategy-activity-action-group=\"\\${section}\"] .trade-row[data-strategy-activity-row-id]\\`,\n                    ) ?? [],\n                  )\n                    .map((node) => {\n                      const rowId = node.getAttribute('data-strategy-activity-row-id') ?? '';\n                      const rowBadgeKeys = distinctValues(\n                        Array.from(node.querySelectorAll('.console-tag--warn'))\n                          .map((badgeNode) => mapSectionBadgeKey(section, badgeNode.textContent?.trim() ?? ''))\n                          .filter(Boolean),\n                      );\n                      return rowId && rowBadgeKeys.length ? \\`\\${rowId} => \\${rowBadgeKeys.join(', ')}\\` : '';\n                    })\n                    .filter(Boolean),\n                ]),\n              );\n              const sectionStructuredRowBadgeBindingCountsBySection = Object.fromEntries(\n                actionGroups.map((section) => [\n                  section,\n                  sectionRawRowBadgeBindingsBySection[section]?.length ?? 0,\n                ]),\n              );\n              const sectionRawRowSemanticBindingsBySection = Object.fromEntries(\n                actionGroups.map((section) => [\n                  section,\n                  Array.from(\n                    panel?.querySelectorAll(\n                      \\`[data-strategy-activity-action-group=\"\\${section}\"] .trade-row[data-strategy-activity-row-id]\\`,\n                    ) ?? [],\n                  )\n                    .map((node) => buildRowSemanticBinding(section, node))\n                    .filter(Boolean),\n                ]),\n              );\n              const sectionStructuredRowSemanticBindingCountsBySection = Object.fromEntries(\n                actionGroups.map((section) => [\n                  section,\n                  sectionRawRowSemanticBindingsBySection[section]?.length ?? 0,\n                ]),\n              );\n              const sectionRowIdsBySection = Object.fromEntries(\n                actionGroups.map((section) => [\n                  section,\n                  distinctValues(sectionRawRowIdsBySection[section]),\n                ]),\n              );\n              const sectionRowTitlesBySection = Object.fromEntries(\n                actionGroups.map((section) => [\n                  section,\n                  sectionRawRowPrimaryTextsBySection[section] ?? [],\n                ]),\n              );\n              const sectionRowTitleBindingsBySection = Object.fromEntries(\n                actionGroups.map((section) => [\n                  section,\n                  distinctValues(sectionRawRowTitleBindingsBySection[section]),\n                ]),\n              );\n              const sectionRowSummaryBindingsBySection = Object.fromEntries(\n                actionGroups.map((section) => [\n                  section,\n                  distinctValues(sectionRawRowSummaryBindingsBySection[section]),\n                ]),\n              );\n              const sectionRowActionBindingsBySection = Object.fromEntries(\n                actionGroups.map((section) => [\n                  section,\n                  distinctValues(sectionRawRowActionBindingsBySection[section]),\n                ]),\n              );\n              const sectionRowBadgeLabelBindingsBySection = Object.fromEntries(\n                actionGroups.map((section) => [\n                  section,\n                  distinctValues(sectionRawRowBadgeLabelBindingsBySection[section]),\n                ]),\n              );\n              const sectionRowUnknownBadgeBindingsBySection = Object.fromEntries(\n                actionGroups.map((section) => [\n                  section,\n                  distinctValues(sectionRawRowUnknownBadgeBindingsBySection[section]),\n                ]),\n              );\n              const sectionRowBadgeBindingsBySection = Object.fromEntries(\n                actionGroups.map((section) => [\n                  section,\n                  distinctValues(sectionRawRowBadgeBindingsBySection[section]),\n                ]),\n              );\n              const sectionRowSemanticBindingsBySection = Object.fromEntries(\n                actionGroups.map((section) => [\n                  section,\n                  distinctValues(sectionRawRowSemanticBindingsBySection[section]),\n                ]),\n              );\n              const sectionDuplicateRowIdsBySection = Object.fromEntries(\n                actionGroups.map((section) => [\n                  section,\n                  duplicateValues(sectionRawRowIdsBySection[section]),\n                ]),\n              );\n              const sectionEmptyStateVisibleSections = actionGroups.filter((section) =>\n                Boolean(panel?.querySelector(\\`[data-strategy-activity-action-group=\"\\${section}\"] .empty-state\\`)),\n              );\n              const topActionDetailsByGroup = Object.fromEntries(\n                topActionGroups.map((group) => {\n                  const buttons = Array.from(\n                    panel?.querySelectorAll(\\`[data-strategy-activity-top-actions=\"\\${group}\"] button\\`) ?? [],\n                  );\n                  const labels = distinctValues(\n                    buttons\n                      .map((node) => node.textContent?.trim() ?? '')\n                      .filter(Boolean),\n                  );\n                  const keys = distinctValues(\n                    buttons\n                      .map((node) => node.getAttribute('data-strategy-activity-top-action-key') ?? '')\n                      .filter(Boolean),\n                  );\n                  const unknownLabels = distinctValues(\n                    buttons\n                      .filter((node) => !(node.getAttribute('data-strategy-activity-top-action-key') ?? '').trim())\n                      .map((node) => node.textContent?.trim() ?? '')\n                      .filter(Boolean),\n                  );\n                  return [\n                    group,\n                    {\n                      labels,\n                      keys,\n                      unknownLabels,\n                    },\n                  ];\n                }),\n              );\n              const topActionLabelsByGroup = Object.fromEntries(\n                topActionGroups.map((group) => [group, topActionDetailsByGroup[group]?.labels ?? []]),\n              );\n              const topActionKeysByGroup = Object.fromEntries(\n                topActionGroups.map((group) => [group, topActionDetailsByGroup[group]?.keys ?? []]),\n              );\n              const topUnknownActionLabelsByGroup = Object.fromEntries(\n                topActionGroups.map((group) => [group, topActionDetailsByGroup[group]?.unknownLabels ?? []]),\n              );\n              const topActionVisibleGroups = topActionGroups.filter((group) =>\n                Boolean(panel?.querySelector(\\`[data-strategy-activity-top-actions=\"\\${group}\"]\\`)),\n              );\n              const sectionActionVisibleSections = actionGroups.filter((section) =>\n                Boolean(panel?.querySelector(\\`[data-strategy-activity-action-group=\"\\${section}\"]\\`)),\n              );\n              return {\n                activeSection:\n                  document.querySelector('.nav-item.active')?.getAttribute('data-nav-section') ?? null,\n                selectedStrategyName:\n                  document.querySelector('[data-strategy-current-panel=\"1\"]')?.getAttribute('data-strategy-current-name') ??\n                  panelBody?.getAttribute('data-strategy-activity-strategy-name') ??\n                  document.querySelector('[data-strategy-current-panel=\"1\"] .panel-head h3')?.textContent?.trim() ??\n                  panel?.querySelector('.floating-panel__head h3')?.textContent?.trim() ??\n                  null,\n                panelVisible: Boolean(panel),\n                panelTitle: panel?.querySelector('.floating-panel__head h3')?.textContent?.trim() ?? null,\n                queryEnabled: panelBody?.getAttribute('data-strategy-activity-query-enabled') ?? null,\n                queryStatus: panelBody?.getAttribute('data-strategy-activity-query-status') ?? null,\n                fetchStatus: panelBody?.getAttribute('data-strategy-activity-fetch-status') ?? null,\n                queryStrategyId: panelBody?.getAttribute('data-strategy-activity-strategy-id') ?? null,\n                queryError: panelBody?.getAttribute('data-strategy-activity-query-error') ?? null,\n                headTrackButton: Boolean(panel?.querySelector('[data-strategy-activity-head-action=\"track\"]')),\n                topRecentActionCount:\n                  panel?.querySelectorAll('[data-strategy-activity-top-actions=\"recent\"] button').length ?? 0,\n                noteCount: panel?.querySelectorAll('.panel-note').length ?? 0,\n                topRecentActionLabels: Array.from(\n                  panel?.querySelectorAll('[data-strategy-activity-top-actions=\"recent\"] button') ?? [],\n                )\n                  .map((node) => node.textContent?.trim() ?? '')\n                  .filter(Boolean),\n                topRecentActionKeys: Array.from(\n                  panel?.querySelectorAll('[data-strategy-activity-top-actions=\"recent\"] [data-strategy-activity-top-action-key]') ?? [],\n                )\n                  .map((node) => node.getAttribute('data-strategy-activity-top-action-key') ?? '')\n                  .filter(Boolean),\n                topActionVisibleGroups,\n                topActionLabelsByGroup,\n                topActionKeysByGroup,\n                topUnknownActionLabelsByGroup,\n                panelNoteTexts: Array.from(panel?.querySelectorAll('.panel-note') ?? [])\n                  .map((node) => node.textContent?.trim() ?? '')\n                  .filter(Boolean)\n                  .slice(0, 12),\n                panelNoteTextsWithoutStructuredKeys: Array.from(panel?.querySelectorAll('.panel-note') ?? [])\n                  .filter((node) => !node.getAttribute('data-strategy-activity-note-key') && !node.closest('.trade-row'))\n                  .map((node) => node.textContent?.trim() ?? '')\n                  .filter(Boolean)\n                  .slice(0, 12),\n                panelNoteEntries: Array.from(\n                  panel?.querySelectorAll('[data-strategy-activity-note-key]') ?? [],\n                )\n                  .map((node) => ({\n                    key: node.getAttribute('data-strategy-activity-note-key') ?? '',\n                    text: node.textContent?.trim() ?? '',\n                  }))\n                  .filter((entry) => entry.key && entry.text)\n                  .slice(0, 12),\n                panelNoteEntryKeys: Array.from(\n                  panel?.querySelectorAll('[data-strategy-activity-note-key]') ?? [],\n                )\n                  .map((node) => node.getAttribute('data-strategy-activity-note-key') ?? '')\n                  .filter(Boolean)\n                  .slice(0, 12),\n                sectionActionVisibleSections,\n                sectionActionLabelsBySection,\n                sectionActionKeysBySection,\n                sectionUnknownActionLabelsBySection,\n                sectionBadgeLabelsBySection,\n                sectionBadgeKeysBySection,\n                sectionUnknownBadgeLabelsBySection,\n                sectionRowCounts,\n                sectionStructuredRowTitleCountsBySection,\n                sectionStructuredRowTitleBindingCountsBySection,\n                sectionStructuredRowSummaryBindingCountsBySection,\n                sectionStructuredRowActionBindingCountsBySection,\n                sectionStructuredRowBadgeBindingCountsBySection,\n                sectionStructuredRowSemanticBindingCountsBySection,\n                sectionStructuredRowIdCountsBySection,\n                sectionRowTitlesBySection,\n                sectionRowTitleBindingsBySection,\n                sectionRowSummaryBindingsBySection,\n                sectionRowActionBindingsBySection,\n                sectionRowBadgeLabelBindingsBySection,\n                sectionRowUnknownBadgeBindingsBySection,\n                sectionRowBadgeBindingsBySection,\n                sectionRowSemanticBindingsBySection,\n                sectionRowIdsBySection,\n                sectionDuplicateRowIdsBySection,\n                sectionEmptyStateVisibleSections,\n                sections,\n                labels,\n              };\n            }");
const strategyActivityWaitScriptSource = decodeSmokeScript("async (timeoutMs) => {\n              const requiredSections = ['proposals', 'change-requests', 'backtests', 'tracking-reviews', 'primary-reviews', 'jobs'];\n              const startedAt = Date.now();\n              const readState = ${readStrategyActivityStateScript};\n              const ensurePanelOpened = () => {\n                const strategyNavButton = document.querySelector('[data-nav-section=\"strategy\"]');\n                if (strategyNavButton instanceof HTMLElement && !strategyNavButton.classList.contains('active')) {\n                  strategyNavButton.click();\n                  return false;\n                }\n                const openButton =\n                  document.querySelector('[data-strategy-current-panel=\"1\"] [data-open-strategy-activity=\"1\"]') ??\n                  document.querySelector('[data-open-strategy-activity=\"1\"]');\n                if (!(openButton instanceof HTMLElement)) {\n                  return false;\n                }\n                const panel = document.querySelector('[data-strategy-activity-panel=\"1\"]');\n                if (panel) {\n                  return true;\n                }\n                openButton.click();\n                return true;\n              };\n              while (Date.now() - startedAt <= timeoutMs) {\n                ensurePanelOpened();\n                const state = readState();\n                const querySettled =\n                  state.queryEnabled !== '1' ||\n                  (state.queryStatus === 'success' && state.fetchStatus === 'idle');\n                const ok =\n                  state.activeSection === 'strategy' &&\n                  Boolean(state.selectedStrategyName) &&\n                  state.panelVisible &&\n                  Boolean(state.panelTitle) &&\n                  querySettled &&\n                  state.headTrackButton &&\n                  requiredSections.every((section) => state.sections?.[section]) &&\n                  requiredSections.every((section) => Boolean(state.labels?.[section]));\n                if (ok) {\n                  return {\n                    ok: true,\n                    elapsedMs: Date.now() - startedAt,\n                    state,\n                  };\n                }\n                await new Promise((resolve) => setTimeout(resolve, 250));\n              }\n              return {\n                ok: false,\n                elapsedMs: Date.now() - startedAt,\n                state: readState(),\n              };\n            }");

export function buildStrategyActivitySetupScript(apiBase: string): string {
  return strategyActivitySetupScript.replace("__API_BASE__", JSON.stringify(apiBase));
}

export function buildStrategyActivityReadStateScript(): string {
  return strategyActivityReadStateScript;
}

export function buildStrategyActivityWaitScriptSource(): string {
  return strategyActivityWaitScriptSource;
}
